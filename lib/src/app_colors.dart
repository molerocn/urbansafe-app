import 'package:flutter/material.dart';

/// Definiciones de color centralizadas para la aplicación.
/// Reemplaza estos valores con la paleta de colores que desees usar.
class AppColors {
  AppColors._();

  static const Color primary = Color(0xFF27548A);  // Azul profundo
  static const Color secondary = Color(0xFFDDA853);  // Dorado
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFFC107);
  static const Color error = Color(0xFFF44336);
  static const Color info = Color(0xFF2196F3);
  static const Color background = Color(0xFFFFFFFF);
}
